[package]
name = "nvme"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]


[dependencies]
kernel = { path = "../../kernel" }
kmacro = { path = "../../kmacro" }
pinned-init = { version = "0.0.8", default-features = false, features = ["alloc"] }
