[package]
name = "nvme"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]


[dependencies]
kernel = { path = "../../kernel" }
kmacro = { path = "../../kmacro" }
pinned-init = { git = "https://github.com/Godones/pinned-init", default-features = false, features = ["alloc"] }
